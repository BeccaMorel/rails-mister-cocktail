<h1>New ingredient dose for <%= @cocktail.name %></h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <p><%= f.input :ingredient, collection: @ingredients %>
  <p><%= f.text_field :description %></p>
  <p><%= f.submit %></p>
<%end%> 